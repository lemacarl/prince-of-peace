import './globals.css'
import { Inter } from 'next/font/google'
import { Analytics } from '@vercel/analytics/react';

const inter = Inter({ subsets: ['latin'] })

export const metadata = {
  title: "Prince of Peace – AI bot fine-tuned on the words of Jesus Christ",
  description:
    "Prince of Peace is an AI bot built with the Vercel AI SDK and fine-tuned on the words of Jesus."
}

export default function RootLayout({
  children
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        {children}
        <Analytics />
      </body>
    </html>
  )
}
